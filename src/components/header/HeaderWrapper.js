import styled from "styled-components";

const HeaderWrapper = styled.div`
  p,
  svg {
    font-weight: 900;
    font-size: 40px;
    letter-spacing: 1px;
  }
  .mainText {
    font-weight: 600;
    font-size: 30px;
  }
`;

export default HeaderWrapper;
